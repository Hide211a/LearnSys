# Локальна розробка
DATABASE_URL="file:./dev.db"
JWT_SECRET="change-me-in-production"
PORT=3001

# Продакшен (Railway): URL фронтенду на Vercel, через кому якщо кілька
# FRONTEND_URL="https://your-app.vercel.app"

# Railway + volume для SQLite (рекомендовано для диплому):
# DATABASE_URL="file:/data/kuzgym.db"

# Або PostgreSQL на Railway (надійніше, змініть provider у schema.prisma на postgresql):
# DATABASE_URL="postgresql://user:pass@host:5432/railway"

# Одноразове наповнення БД після деплою (release command)
# RUN_SEED=true
